CREATE TABLE Employees (
    Id UNIQUEIDENTIFIER NOT NULL,
    FirstName NVARCHAR(25) NOT NULL,
    LastName NVARCHAR(25) NOT NULL,
    Address NVARCHAR(100) NOT NULL,
    Email NVARCHAR(50) NOT NULL,
    LastUpdated DATETIME2 NOT NULL,
    CONSTRAINT PK_Employees_Id PRIMARY KEY CLUSTERED (Id)
);

CREATE TABLE EmployeeExports (
    Id UNIQUEIDENTIFIER NOT NULL,
    ExtractDate DATETIME2 NOT NULL,
    CONSTRAINT PK_EmployeeExports_Id PRIMARY KEY CLUSTERED (Id)
);

INSERT INTO EmployeeExports
    (Id, ExtractDate)
    VALUES('00000000-0000-0000-0000-000000000000', '20000101 12:00:00 AM');